import { serve } from '@hono/node-server';
import app from './index';

const port = process.env.HONO_PORT || 3002;

console.log(`🚀 Hono server is running on port ${port}`);

serve({
  fetch: app.fetch,
  port: Number(port),
}); 